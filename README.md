# 미션3 기능 체크리스트

## 1. 기본 실행 / 모드 선택

- [x] `python main.py`로 정상 실행됨
- [x] 사용자 입력(3×3) / `data.json` 분석 모드를 선택할 수 있음
- [x] 외부 라이브러리 없이 Python 표준 라이브러리만 사용함

## 2. 사용자 입력 모드 — 3×3

- [x] 필터 A, 필터 B, 패턴을 각각 3×3으로 입력받음
- [x] 행/열 개수 또는 숫자 입력이 잘못되면 재입력할 수 있음
- [x] 두 필터의 MAC 점수를 계산함
- [x] epsilon 기준으로 A / B / 판정 불가를 결정함
- [x] 3×3 MAC 평균 연산 시간을 출력함

## 3. MAC 연산

- [x] 외부 라이브러리 없이 2중 반복문으로 위치별 `곱셈 + 누적`을 구현함
- [x] 3×3뿐 아니라 N×N 배열에서도 같은 MAC 함수를 사용할 수 있음
- [x] 점수 비교 시 `abs(score_a - score_b) < epsilon` 정책을 사용함

## 4. data.json 분석

- [x] `data.json`에서 filters와 patterns를 정상적으로 읽음
- [x] `size_5_1`, `size_13_1` 등의 키에서 N을 추출하여 알맞은 필터를 선택함
- [x] `+ / cross → Cross`, `x → X` 형태로 라벨을 정규화함
- [x] 패턴과 필터의 N×N 크기가 맞는지 검증함
- [x] 잘못된 케이스 하나 때문에 프로그램 전체가 종료되지 않음

## 5. JSON 케이스 판정

- [x] 각 패턴에 대해 Cross/X MAC 점수를 계산함
- [x] Cross / X / UNDECIDED를 판정함
- [x] expected와 비교하여 PASS / FAIL을 출력함
- [ ] FAIL인 경우 케이스와 실패 이유를 남김

## 6. 성능 분석

- [ ] 3×3 / 5×5 / 13×13 / 25×25를 측정함
- [ ] 각 크기마다 MAC 연산을 최소 10회 실행하여 평균 시간을 구함
- [ ] I/O 시간이 아니라 MAC 연산 구간을 중심으로 측정함
- [ ] `크기 / 평균 시간(ms) / N² 연산 횟수`를 출력함

## 7. 결과 요약

- [ ] 전체 테스트 수 / PASS / FAIL 수를 출력함
- [ ] 실패 케이스가 있으면 케이스명과 이유를 출력함

## 8. README

- [ ] 실행 방법을 작성함
- [ ] MAC 연산과 epsilon 판정 방식을 간단히 설명함
- [ ] 라벨 정규화를 한 이유를 설명함
- [ ] FAIL이 있다면 원인을 분석하고, 없다면 실패가 0인 이유를 설명함
- [ ] N×N에서 N²번 연산하므로 시간복잡도가 `O(N²)`임을 설명함

## 9. 보너스 — 시간 남으면

- [ ] N을 입력받아 Cross / X 패턴을 자동 생성함
- [ ] 생성기를 N=3으로 고정하지 않고 다양한 N에서 사용할 수 있게 함
- [ ] 생성된 배열을 기존 MAC/성능 분석에 재사용함
- [ ] 여유가 더 있으면 2차원 → 1차원 배열 변환 전후 성능을 비교함

---

# 프로그램 전체 흐름도

```
python main.py
      │
      ▼
  모드 선택
   /     \
  /       \
1번       2번
│          │
│          ▼
│      data.json 읽기
│          │
│      patterns 반복
│          │
│      size 추출
│          │
│      라벨 정규화
│          │
│      크기 검증
│          │
│        MAC
│          │
│       판정
│          │
│    PASS / FAIL
│          │
│      결과 요약
│          │
│     성능 분석
│
▼
3×3 직접 입력
│
├─ 필터 A
├─ 필터 B
└─ 패턴
      │
      ▼
    MAC 2번
      │
      ▼
epsilon 비교
      │
      ▼
A / B /
UNDECIDED
      │
      ▼
3×3 시간 측정
```

## 모드 1

```
3×3 A 입력
     ↓
3×3 B 입력
     ↓
3×3 pattern 입력
     ↓
MAC(pattern, A)
MAC(pattern, B)
     ↓
epsilon 비교
     ↓
A / B / UNDECIDED
     ↓
시간 출력
```

## 모드 2

```
data.json
    ↓
filters / patterns
    ↓
size_5_1
    ↓
5 추출
    ↓
size_5 필터 선택
    ↓
+ → Cross
x → X
    ↓
5×5 검증
    ↓
Cross MAC / X MAC
    ↓
epsilon 판정
    ↓
expected 비교
    ↓
PASS / FAIL
    ↓
다음 pattern
    ↓
전체 결과 요약
    ↓
3 / 5 / 13 / 25 성능 측정
```
